# 产品想法：AI 图片"反渲染"为可编辑 HTML / 矢量

> 一句话：把已经做好的 PPT 风格图片（尤其是 AI 生成的）导入，让 AI 分析并**重建成结构化、可编辑的 HTML/矢量**，从而接入本工具的可视化编辑能力——修复损坏文字、调整框/箭头/图表/配图。
>
> 状态：**想法 / 待落地**。本文档为后续实现的设计存档。

---

## 1. 背景与痛点

- 市面上有 OpenAI 的图像模型、Google 的 Nano Banana 等顶级绘图模型，很多人用它们直接生成 PPT 风格的图片。
- 但生成结果常有问题：**细节错误、文字损坏/乱码**，需要靠自然语言反复对话调整，几轮下来仍然改不好。
- 根因：图片是**栅格化的死图**，无法精确、确定性地局部编辑；自然语言对像素级修改是低带宽、不可控的交互方式。
- 现状的本工具只能导入 **HTML**。如果能再导入这类 **AI 生成的图片**并变回可编辑，价值会大很多。

## 2. 核心思路：不是"克隆"，是"反渲染（de-rendering）"

**最容易踩的坑**：把目标定成"像素级还原"。这条路不可行也没必要——

- 字体已栅格化，专有字体拿不回来 → 只能映射到最近的 web 字体；
- 渐变 / 阴影 / 发光 / 3D 只能近似成 CSS，有损；
- 照片质感的内容（AI 生成的人物、纹理背景）**根本无法矢量化**。

**正确目标**：重建一个"看起来够接近、但每个元素都真正可编辑"的近似版，作为人工微调的**起点**。把预期对齐到这里，难度立刻下降一个量级。

### 关键洞察：坏字从 bug 变卖点
因为是"重绘"而不是"复刻"，所以可以让 VLM **结合整页语境推断这段文字本来应该是什么**，再重新排版，而不是忠实复刻乱码。自然语言对话改不动的东西，在这里变成"盖掉旧像素 + 重打一行字"。

## 3. 与本工具架构的契合点（为什么特别适合）

本编辑器没有自己的元素 schema，它的"数据模型"**就是 HTML/DOM 本身**：一个 slide = 一棵 `HTMLElement` 树，编辑 = 改 inline style，导出 = 干净 HTML。

所以"图片 → 可编辑"可降维成一个边界清晰的问题：**图片 → 一段结构化、绝对定位的干净 HTML slide**。生成出来的 HTML 直接喂给现有的 `loadHtml()`，已有的**选择 / 拖动 / 缩放 / 样式面板 / 撤销重做 / 多页**能力全部白嫖、零引擎改动。

要造的只是一个**纯外挂的前置"翻译器"**，失败也不污染主引擎，可独立迭代。

## 4. 推荐路线：背景兜底 + 渐进式矢量化（overlay-first）

不要一上来追求"整图拆成纯矢量"。最低风险、最快见效、又能平滑演进的策略是**分层**：

| 层 | 内容 | 保真度 | 实现难度 |
|---|---|---|---|
| **L0** | 原始图整张当背景 | 像素级完美 | 零 |
| **L1** | 文字 overlay（OCR 框 + VLM 纠正后的正确文字） | 高 | 低 |
| **L2** | 形状 / 箭头 / 框从背景"抠"成矢量 | 中 | 中（要补洞 inpainting） |
| **L3** | 完全重建，丢弃背景 | 取决于模型 | 高 |

**MVP 只做 L0 + L1** 就能解决约 80% 的痛点：

> 原图当背景 → OCR 拿到每段文字的精确包围盒 + 采样颜色/字号/对齐 → VLM 判断哪些文字是"坏的"并给出正确文本 → 在对应位置生成可编辑的绝对定位 `<div>`。用户要改某段文字时，**用采样到的局部背景色画一个矩形盖掉原像素，再把新文字渲染上去**。

这一步几乎没有"还原失败"风险：背景永远是原图、永远好看；可编辑部分是可控地叠上去的。直接命中"对损坏的文字进行调整"。

**L2**（把框、箭头从背景抠出来变成真矢量）是难点，核心难在：**抠走一个元素后，它背后那块洞要补上（inpainting）**——需要 LaMa / SD-inpaint 等。MVP 阶段先不抠，只叠加，把这个难题推迟。

## 5. 按元素类型的策略

| 元素 | 难度 | 策略 |
|---|---|---|
| **文字** | 低（最成熟） | OCR（PaddleOCR / PP-Structure / 云端 Vision）给框和内容，VLM 做语义纠错与修复 |
| **框 / 色块** | 较低 | 矩形检测 + 采样填充色/描边/圆角 → 重建成带 `border-radius`/`background` 的 div |
| **箭头 / 连线** | 中 | 传统 CV 难，VLM 擅长语义识别"A 指向 B" → 重建成连接器 / SVG line（端点 + 样式） |
| **图表（图）** | 高 | 反推精确数据几乎不可能。v1 先当图片保留，或让用户手动换成真图表组件。**不承诺自动还原数据** |
| **照片 / 纹理** | —— | 不矢量化，裁剪成 raster 片段定位摆放（与"真实可编辑 PPT"结构一致：图片占位 + 文字 + 形状） |

## 6. 提升保真度的关键技巧：自校正渲染环

```
生成 HTML → 用 headless 渲染成 PNG → 把"原图"和"重建图"一起给 VLM：
"对比这两张，列出位置/颜色/文字/缺失元素的差异，输出成修正补丁"
→ 应用补丁 → 再渲染 → 循环 1~3 次
```

把"一次生成、对不齐就完蛋"变成"迭代收敛"。本工具的 iframe 本身就能渲染，截图很容易接。这也是当前 screenshot-to-code 类工具拉开差距的关键。

## 7. 如何接进现有代码

- **入口**：在 `EditorCore` 上加一个 `openFromImage(file)`，与 `openFromFile()` 平级。
- **翻译器输出**：固定画布尺寸（如 1280×720）的根节点 + 一堆**绝对定位、inline style** 的子元素 + 必要的 raster 片段（base64/blob）。正好是 `loadHtml()` 想要的输入形态。
- **坐标**：检测到的像素框 → 归一化到画布坐标 → `position:absolute; left/top/width/height`，与现有 DOM 模型天然对齐。
- **资源**：抠出来的图片片段走已有的 `AssetBundle` blob 机制。
- 之后**一行引擎代码都不用改**——选择/拖动/缩放/样式面板/撤销重做自动生效。

## 8. MVP 分期建议

1. **阶段一（验证假设）**：限定"信息图 / 提案风格"的 slide（框 + 文字 + 箭头 + 图标，不含大面积照片），实现 **L0 背景 + L1 文字纠正 overlay + 自校正环**。成本低，能立刻演示"导入坏字 PPT → 一键变可改"的核心价值。
2. **阶段二**：L2 形状/框矢量化 + inpainting 补洞；箭头/连线语义重建。
3. **阶段三**：L3 全重建（可丢背景）、图表组件化、照片密集型版式的泛化。

## 9. 待决的选型问题

1. **OCR / VLM 放云端还是本地？** 云端（Claude / Gemini / GPT-4V + 云 OCR）效果最好但要联网+付费；本地（PaddleOCR + 本地模型）隐私好但部署重、效果弱。涉及敏感物料时倾向支持私有化/本地部署。
2. **保真 vs 可编辑哪个优先？** 偏保真 → 背景兜底路线；偏"干净可复用 HTML" → 直接全重建路线。
3. **第一版要不要碰箭头/形状**，还是只做文字层。

## 10. 参考技术 / 工具

- **OCR + 版面**：PaddleOCR / PP-Structure（含版面区域）、云端 Vision（Google Vision、Azure Read）。
- **多模态语义理解 + 文字纠错 + 重建**：Claude / Gemini / GPT-4V。
- **矢量化**：vtracer（彩色，Rust）、potrace（单色）。
- **抠图补洞（inpainting）**：LaMa、SD-inpaint。
- **相邻方向参考**：screenshot-to-code 类工具、学术界的 chart/diagram de-rendering（反渲染）研究。
